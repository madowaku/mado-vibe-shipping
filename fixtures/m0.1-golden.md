# M0.1 Golden Fixture

## Input

> I want an app where my partner and I can save places we want to visit.

## Expected intent

```json
{
  "goal": "Keep a shared list of places two people want to visit.",
  "app_type": "shared_list",
  "audience": {
    "kind": "small_private_group",
    "size_hint": 2
  },
  "primary_entity": "place",
  "actions": [
    "add",
    "edit",
    "mark_visited",
    "delete"
  ],
  "device_priority": "mobile",
  "access": {
    "visibility": "invite_only",
    "auth_required": true
  },
  "persistence": {
    "required": true,
    "reason": "The shared list must remain available across sessions and devices."
  },
  "constraints": [],
  "preferences": [],
  "assumptions": [
    {
      "field": "device_priority",
      "value": "mobile",
      "reason": "A visit list is likely to be used while away from a desktop and this choice is reversible.",
      "reversible": true
    },
    {
      "field": "actions",
      "value": ["add", "edit", "mark_visited", "delete"],
      "reason": "These are the minimal lifecycle actions for a useful shared list and are reversible product decisions.",
      "reversible": true
    }
  ],
  "open_questions": []
}
```

## Pass conditions

- No framework/database/hosting question is asked.
- The result clearly separates user facts from inferred defaults.
- The output is sufficient for a planner to begin implementation.
- Low-risk reversible choices are defaults, not interruptions.
